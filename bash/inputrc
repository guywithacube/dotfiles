# https://www.gnu.org/software/bash/manual/html_node/Readline-Init-File.html

#==========
# Color
#==========
# Colorize autocompleted text
set colored-stats on

#==========
# Vi editing
#==========
# Set editing mode to use vi style
set editing-mode vi
# Only give 0.15 sec for ambiguous key sequences
set keyseq-timeout 150

# Show the current mode in the prompt
set show-mode-in-prompt on

# Add string to beginning of prompt to show Vi status
# Insert mode
set vi-ins-mode-string ""
# Normal mode
# readline  does NOT have an OR operator; must explicitly duplicate
$if term=xterm-256color
	set vi-cmd-mode-string "\e[1;7m"
$else term=screen-256color
	set vi-cmd-mode-string "\e[1;7m"
$endif
